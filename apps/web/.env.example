# Only needed when the web app is deployed separately from the API (e.g. Vercel +
# Render). Point it at the deployed API's public URL. Not needed for local dev.
# VITE_API_URL="https://your-api.onrender.com"
